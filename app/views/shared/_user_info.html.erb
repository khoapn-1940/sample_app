<%= link_to gravatar_for(current_user, size: Settings.gravatar_forSize2), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("chap13.viewprofile"), current_user %></span>
<span><%= pluralize(current_user.microposts.count, "micropost") %></span>
